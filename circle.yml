machine:
  services:
    - docker

dependencies:
  override:
    - docker info
    - docker build -t r.iadops.com/dockerhello .

test:
  override:
    - docker run r.iadops.com/dockerhello true

deployment:
  hub:
    branch: master
    commands:
      - docker push r.iadops.com/dockerhello
