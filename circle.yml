machine:
  services:
    - docker

dependencies:
  override:
    - docker info
    - docker build -t jesse/dockerhello .

test:
  override:
    - docker run jesse/dockerhello true

deployment:
  hub:
    branch: master
    commands:
      - docker push jesse/dockerhello
